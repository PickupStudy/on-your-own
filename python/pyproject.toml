[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pkb-processor"
version = "0.1.0"
description = "Personal knowledge base processor"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.28.0",
  "pymysql>=1.1.0"
]
